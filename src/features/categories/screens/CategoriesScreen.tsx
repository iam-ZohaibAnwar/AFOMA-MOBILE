/**
 * Legacy export — Browse tab now uses CategoryBrowserScreen (two-pane navigator).
 * Kept so older imports / hot-reload bundles still resolve.
 */
export { CategoryBrowserScreen as CategoriesScreen } from './CategoryBrowserScreen';
