export const ADMIN_COMMISSION_RATE_MIN = 0;
export const ADMIN_COMMISSION_RATE_MAX = 9;

/** Web parity — client-only; backend does not enforce on staging. */
export const ADMIN_FEATURED_SHOPS_MAX = 3;
